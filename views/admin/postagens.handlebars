<h2>Lista de Postagens:</h2>
<hr>    
<a href="/admin/postagens/add">
    <button class="btn btn-success">Nova Postagem</button>
</a>

{{#each postagens}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{titulo}}</h4>
            <p>{{descricao}}</p>
            <small>Data: {{data}}</small><br>
            <span class="badge text-bg-success">{{categoria.nome}}</span>
        </div>
    </div>
{{else}}

{{/each}}